<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Nova Extranet</title>
    <link rel="stylesheet" href="<?php echo ruta; ?>include/css/main.css">
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>Nova Extranet</h1>
            <div class="user-info">
                <span><?php 
                // Función para limpiar emojis
                function limpiarEmojisHeader($texto) {
                    if (empty($texto)) return $texto;
                    return preg_replace('/[\x{1F300}-\x{1F9FF}]/u', '', $texto);
                }
                echo htmlspecialchars(limpiarEmojisHeader($_SESSION['nombre'])); 
                ?></span>
                <a href="<?php echo ruta; ?>?pagina=logout" class="btn-logout">Cerrar Sesión</a>
            </div>
        </header>
        
        <nav class="nav">
            <a href="<?php echo ruta; ?>?pagina=inicio" class="nav-link active">Inicio</a>
            <a href="<?php echo ruta; ?>?pagina=clientes" class="nav-link">Clientes</a>
            <a href="<?php echo ruta; ?>?pagina=ordenes" class="nav-link">Órdenes</a>
            <a href="<?php echo ruta; ?>?pagina=pdf" class="nav-link">PDFs</a>
        </nav>
        
        <main class="main-content">
            <h2>Dashboard</h2>
            
            <div class="stats-grid">
                <div class="stat-card">
                    <h3>Total Clientes</h3>
                    <p class="stat-number"><?php echo $stats['total_clientes']; ?></p>
                </div>
                <div class="stat-card">
                    <h3>Total PDFs</h3>
                    <p class="stat-number"><?php echo $stats['total_pdfs']; ?></p>
                </div>
                <div class="stat-card">
                    <h3>PDFs Aprobados</h3>
                    <p class="stat-number"><?php echo $stats['pdfs_aprobados']; ?></p>
                </div>
            </div>
        </main>
    </div>
</body>
</html>


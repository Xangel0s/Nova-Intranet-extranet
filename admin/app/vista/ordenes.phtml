<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Órdenes - Nova Extranet</title>
    <link rel="stylesheet" href="<?php echo ruta; ?>include/css/main.css">
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>Nova Extranet</h1>
            <div class="user-info">
                <span><?php 
                // Función para limpiar emojis
                function limpiarEmojisHeader($texto) {
                    if (empty($texto)) return $texto;
                    return preg_replace('/[\x{1F300}-\x{1F9FF}]/u', '', $texto);
                }
                echo htmlspecialchars(limpiarEmojisHeader($_SESSION['nombre'])); 
                ?></span>
                <a href="<?php echo ruta; ?>?pagina=logout" class="btn-logout">Cerrar Sesión</a>
            </div>
        </header>
        
        <nav class="nav">
            <a href="<?php echo ruta; ?>?pagina=inicio" class="nav-link">Inicio</a>
            <a href="<?php echo ruta; ?>?pagina=clientes" class="nav-link">Clientes</a>
            <a href="<?php echo ruta; ?>?pagina=ordenes" class="nav-link active">Órdenes</a>
            <a href="<?php echo ruta; ?>?pagina=pdf" class="nav-link">PDFs</a>
        </nav>
        
        <main class="main-content">
            <h2>Órdenes</h2>
            
            <?php if (empty($ordenes)): ?>
                <div class="empty-state">
                    <p>No hay órdenes disponibles</p>
                </div>
            <?php else: ?>
                <div class="table-container">
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>Cliente</th>
                                <th>PDF</th>
                                <th>Estado</th>
                                <th>Fecha</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php 
                            // Función para limpiar emojis de los datos
                            function limpiarEmojis($texto) {
                                if (empty($texto)) return $texto;
                                return preg_replace('/[\x{1F300}-\x{1F9FF}]/u', '', $texto);
                            }
                            
                            foreach ($ordenes as $orden): ?>
                                <tr>
                                    <td><?php echo htmlspecialchars(limpiarEmojis($orden['razon_social'] ?? 'N/A')); ?></td>
                                    <td><?php echo htmlspecialchars(limpiarEmojis($orden['pdf'])); ?></td>
                                    <td><?php echo $orden['estado'] ? 'Aprobado' : 'Observado'; ?></td>
                                    <td><?php echo date('d/m/Y', strtotime($orden['fecha_creacion'])); ?></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            <?php endif; ?>
        </main>
    </div>
</body>
</html>


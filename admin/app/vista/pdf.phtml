<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PDFs - Nova Extranet</title>
    <link rel="stylesheet" href="<?php echo ruta; ?>include/css/main.css">
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>Nova Extranet</h1>
            <div class="user-info">
                <span><?php echo htmlspecialchars($_SESSION['nombre']); ?></span>
                <a href="<?php echo ruta; ?>?pagina=logout" class="btn-logout">Cerrar Sesión</a>
            </div>
        </header>
        
        <nav class="nav">
            <a href="<?php echo ruta; ?>?pagina=inicio" class="nav-link">Inicio</a>
            <a href="<?php echo ruta; ?>?pagina=clientes" class="nav-link">Clientes</a>
            <a href="<?php echo ruta; ?>?pagina=ordenes" class="nav-link">Órdenes</a>
            <a href="<?php echo ruta; ?>?pagina=pdf" class="nav-link active">PDFs</a>
        </nav>
        
        <main class="main-content">
            <h2>Documentos PDF</h2>
            
            <?php if (empty($pdfs)): ?>
                <div class="empty-state">
                    <p>No hay documentos PDF disponibles</p>
                </div>
            <?php else: ?>
                <div class="table-container">
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>Cliente</th>
                                <th>Archivo</th>
                                <th>Estado</th>
                                <th>Vista</th>
                                <th>Fecha Subida</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($pdfs as $pdf): ?>
                                <tr>
                                    <td><?php echo htmlspecialchars($pdf['razon_social'] ?? 'N/A'); ?></td>
                                    <td><?php echo htmlspecialchars($pdf['pdf']); ?></td>
                                    <td><?php echo $pdf['estado'] ? 'Aprobado' : 'Observado'; ?></td>
                                    <td><?php echo $pdf['vista'] ? 'Sí' : 'No'; ?></td>
                                    <td><?php echo $pdf['fecha_subida'] ? date('d/m/Y H:i', strtotime($pdf['fecha_subida'])) : 'N/A'; ?></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            <?php endif; ?>
        </main>
    </div>
</body>
</html>


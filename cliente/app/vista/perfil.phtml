<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil - Nova Intranet</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="<?php echo ruta; ?>include/css/main.css">
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>Nova Intranet</h1>
            <div class="user-info">
                <span><?php 
                // Función para limpiar emojis
                function limpiarEmojisHeader($texto) {
                    if (empty($texto)) return $texto;
                    return preg_replace('/[\x{1F300}-\x{1F9FF}]/u', '', $texto);
                }
                echo htmlspecialchars(limpiarEmojisHeader($_SESSION['razon_social'])); 
                ?></span>
                <a href="<?php echo ruta; ?>?pagina=logout" class="btn-logout">Cerrar Sesión</a>
            </div>
        </header>
        
        <nav class="nav">
            <a href="<?php echo ruta; ?>?pagina=ordenes" class="nav-link">Órdenes</a>
            <a href="<?php echo ruta; ?>?pagina=perfil" class="nav-link active">Perfil</a>
        </nav>
        
        <main class="main-content">
            <h2>Mi Perfil</h2>
            
            <?php 
            if ($perfil): ?>
                <div class="profile-info">
                    <p><i class="fa fa-id-card"></i> <strong>RUC:</strong> <?php echo htmlspecialchars(limpiarEmojisHeader($perfil['ruc'])); ?></p>
                    <p><i class="fa fa-building"></i> <strong>Razón Social:</strong> <?php echo htmlspecialchars(limpiarEmojisHeader($perfil['razon_social'])); ?></p>
                    <p><i class="fa fa-envelope"></i> <strong>Correo:</strong> <?php echo htmlspecialchars(limpiarEmojisHeader($perfil['correo'] ?? 'N/A')); ?></p>
                    <p><i class="fa fa-phone"></i> <strong>Teléfono:</strong> <?php echo htmlspecialchars(limpiarEmojisHeader($perfil['telefono'] ?? 'N/A')); ?></p>
                    <p><i class="fa fa-map-marker-alt"></i> <strong>Dirección:</strong> <?php echo htmlspecialchars(limpiarEmojisHeader($perfil['direccion'] ?? 'N/A')); ?></p>
                </div>
            <?php else: ?>
                <div class="empty-state">
                    <p>No se pudo cargar la información del perfil</p>
                </div>
            <?php endif; ?>
        </main>
    </div>
</body>
</html>

